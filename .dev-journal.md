---
date: "2026-07-15"
last_updated: "$(date '+%Y-%m-%d %H:%M:%S')"
project: "Sedna"
status: "completed"
tags: ["bugfix", "feature", "ops"]
---

### Daily Summary
* **Context:** Full pre-production audit of Sedna Academy — verify no decorative/stub functionality, add real test coverage, and produce an AWS free-tier deployment path.
* **Done:**
  - Audited all 18 backend routers and 21 frontend views (via parallel background agents) for stub/decorative behavior
  - Fixed: team invites now send a real email (`send_invite_email`); webhooks now actually fire on module.completed / cert.earned / quiz.passed / badge.earned / user.invited (previously dead code, never called)
  - Fixed a real data bug: `WebhookEndpoint.events` was stored as a scalar VARCHAR while every caller treated it as a list — webhook creation would have crashed in production. Changed to `ARRAY(String)`, migrated the dev DB in place.
  - Fixed a real crash bug: `cert.tier.cert_name` accessed an unloaded async relationship (MissingGreenlet) whenever a certificate was newly awarded — replaced with the already-loaded `tier` variable.
  - Fixed admin delete endpoints (path/tier/module) crashing with raw 500s (FK violations) when the target had content/progress — now return a clean 400.
  - Removed two cosmetic fabrications in ContentView.vue (fake "Last edited" dates, heuristic-only status) and dead placeholder code in TeamView.vue.
  - Live-walked the app as a real user: video playback, module completion → XP/streak/badge/activity persistence, admin content CRUD all confirmed real end-to-end.
  - Built a full pytest suite from scratch (none existed before) — 29 tests across auth, progress/certs, quizzes, admin CRUD, team invites, settings, webhook delivery. All passing against a real Postgres test DB.
  - Built and locally verified the full production stack: `backend/Dockerfile.prod`, `frontend/Dockerfile.prod` (multi-stage build → Caddy), `docker-compose.prod.yml`, `Caddyfile` (auto-HTTPS + /api reverse proxy), `app/init_db.py` (schema + first admin bootstrap), deploy/backup/ec2-bootstrap scripts. Confirmed a real login works end-to-end through the Caddy proxy locally before writing the guide.
  - Wrote `docs/DEPLOYMENT.md` — full step-by-step AWS free-tier (EC2 + Caddy, no RDS) deployment guide.
* **In Progress:**
  - None — all six audit/fix/test/deploy tasks completed this session.
* **Friction/Blocks:**
  - No `alembic.ini` or migrations exist despite the folder being present — schema is created via `create_all`. Flagged in the deployment guide as something to set up before further schema changes.
  - No `frontend/package-lock.json` — prod Dockerfile uses `npm install` instead of `npm ci`. Flagged as a follow-up.
